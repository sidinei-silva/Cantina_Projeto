<%= form_with(model: pedido, local: true) do |form| %>
  <% if pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pedido.errors.count, "error") %> prohibited this pedido from being saved:</h2>

      <ul>
      <% pedido.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dataPedido %>
    <%= form.text_field :dataPedido %>
  </div>

  <div class="field">
    <%= form.label :statusPedido %>
    <%= form.text_field :statusPedido %>
  </div>

  <div class="field">
    <%= form.label :precoPedido %>
    <%= form.text_field :precoPedido %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
